import MainLayout from "../layout/MainLayout";

const Home = (props) => {
  return (
    <MainLayout>
      <div className="home-wrapper">Home Page</div>
    </MainLayout>
  );
};

export default Home;
